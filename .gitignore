# data — never commit waveforms (GB-scale); they are reproducible via scripts
data/
*.hdf5
*.mseed

# external clones / large downloads
PhaseNet/
*.tar.gz
*.zip

# kaggle raw outputs (keep only what's copied into outputs/)
kaggle/Train_output/

# runtime cache for the local console (copies of docs/index.html + live
# state.json / cwa_events.json that change every second)
outputs/console_web/
outputs/live_web/
outputs/monitor_web/

# scratch / probe files
outputs/*_broken.pt
outputs/probe_*.pt
outputs/ft_eval_v3_fresh.png
zi54Ugiu

# python
__pycache__/
*.pyc
.venv/
venv/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# experiment tracking
wandb/
mlruns/

# ---- portfolio hygiene: local-only, never read at runtime ----
# untrack the already-committed copies once with:
#   git rm -r --cached kaggle extras
#   git rm --cached outputs/*.png outputs/*.gif outputs/magnet_cwa.pt outputs/event_catalog.csv
# KEEP tracked (the audit workflow / VM / console read these):
#   outputs/phasenet_cwa_ft.pt, outputs/magnet_cwa_v2/,
#   outputs/site_terms.json, outputs/audit_archive/, outputs/replay_*.json,
#   assets/, docs/, vercel/, scripts/, src/, web/
kaggle/
extras/
NOTES.md
outputs/*.png
outputs/*.gif
outputs/blind_magnet_*.json
outputs/convergence_*.json
outputs/magnet_cwa.pt
outputs/event_catalog.csv
# ...but the README needs these two figures — keep them tracked
!outputs/convergence_0403.gif
!outputs/eval_cwa_2020_2021_seisbench-phasenet-ft-phasenet_cwa_ft.png
